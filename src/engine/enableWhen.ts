import type { AnswerMap, NormalizedItem } from '../parser/types'

// Stub: real operator + enableBehavior evaluation coming later
export function isItemEnabled(_item: NormalizedItem, _answers: AnswerMap): boolean {
  return true
}
